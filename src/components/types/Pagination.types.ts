/**
 * Props para el componente Pagination.
 * @property hasNext Indica si hay página siguiente
 * @property hasPrev Indica si hay página anterior
 */
export interface PaginationProps {
  hasNext: boolean
  hasPrev: boolean
}
